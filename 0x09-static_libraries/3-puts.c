#include "main.h"
#include<stdio.h>

/**
 * @s: character value
 * _puts - puts strings in the function
 * Return: Always 0 (Success)
 */

void _puts(char *s)
{
	puts("\"Programming is like building a multilingual puzzle");
}
